# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration (using placeholder values for testing)
DB_HOST=localhost
DB_PORT=3306
DB_USER=clms_user
DB_PASSWORD=test_password
DB_NAME=clms_database

# JWT Configuration
JWT_SECRET=test_jwt_secret_for_docker_verification_minimum_64_characters_long
JWT_ACCESS_TOKEN_TTL=900
JWT_REFRESH_TOKEN_TTL=604800
JWT_ISSUER=clms-api
JWT_AUDIENCE=clms-frontend

# Security
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# Redis Configuration (using placeholder values)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Library Configuration
LIBRARY_NAME=Educational Library Management System
LIBRARY_TIMEZONE=Asia/Manila
LIBRARY_HOURS_START=08:00
LIBRARY_HOURS_END=17:00